prompt_function_guide = """
# 指令
我有一个APP，里面有些一、二、三级功能模块，请根据用户查询的问题，给用户推荐不同的一级功能模块的标签。

# 要求：
推荐的一级功能模块标签最多不会超过2个，不同的功能标签用<逗号>隔开。
不要输出推荐的一级功能标签之外的其他内容，不要解释。

# 一、二、三级功能模块列表和描述
{function_module}

# 可以被推荐的一级功能模块标签包含:
{function_tag}

# 当前用户的问题
{query}


# 输出：

"""


prompt_router = """
# 指令
我有一个APP，里面有些一、二、三级功能模块，请根据用户查询的问题，判断用户的问题是否与APP里的功能模块有关或者用户的问题能够通过这些功能模块解决。
如果可以，请返回True，否则返回False。

# 功能模块列表和描述
{function_module}

# 当前用户的问题
{query}

# 输出：
"""
